# 赤模様を目の縁へ接続

face_scale_20260917 の90%縮小済みモデルを基準に、上下4本の赤模様の目側の端だけを延長。仮面・顔パーツの形状、サイズ、配置は変更なし。

既存の投影UVテクスチャ上で目の楕円に沿って接続し、黒い目の面の裏へ少量重ねる。下側は元の模様の幅・流れを保つため延長量を制限。

`extend_red.py` → `export_verify.py` で再生成可能。テクスチャはblend/GLB内蔵。正面・左右斜め・側面・全身をGLB再読込後に確認。

2026-09-17 本番採用。キャッシュ識別子 `v3-face-red-20260917`。
