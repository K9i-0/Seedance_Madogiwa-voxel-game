# そば屋ハザード共通アイテム

`items.glb`: Handgun / Shotgun / GreenHerb / RedHerb / YellowHerb / AmmoBox / ShellBox / Key / Crate / Barrel。768三角面・11材質。`tools/build_hazard_village.py` で生成。銃はBlender -Y前方、Z上向き、グリップ付近を原点とする。ビールは既存 `../beer_mug_v2/beer_mug.glb` を共有する。
