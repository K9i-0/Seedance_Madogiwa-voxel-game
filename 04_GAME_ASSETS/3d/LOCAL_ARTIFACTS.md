# ローカル制作途中ファイルの保管

2026-09-17、ユーザー依頼で未追跡172ファイル（518,846,491 bytes）を整理。ファイルの削除は行っていない。移動前後の全ファイルをSHA-256で照合済み。

## 保管先

170ファイルはリポジトリ内の `.local/artifact-archive/20260917-sobaya-v3/` へ移動。元のリポジトリ相対パスをそのまま下位ディレクトリとして保持する。

ローカルの `inventory.json` に元パス、保管パス、容量、SHA-256、処理区分を記録。必要な旧版を使う場合は台帳で保管先を確認し、必要なファイルだけ元パスまたは別の作業ディレクトリへコピーして利用する。

保管内容はWan四面図の重複4枚、仮面・全身の旧モデル／監査資料、Blenderバックアップ、個別QA画像、別件の `output/imagegen/sobaya_attendance_notice/`。採用済みのモデル・コード・入力・まとめ画像・制作記録は元の正本位置でGit管理を維持。

`.local/` はGit対象外のため、この保管領域と台帳はpushやcloneでは同期されない。別環境でも旧版を必要とする場合は保管フォルダを別途コピーする。

## 元の場所に維持した2ファイル

以下は段階的なビルドが参照する再生成可能な中間ファイルのため、移動せず、パスを限定して `.gitignore` へ登録した。

- `characters/sobaya/rig_v3_20260917/posture.blend`
- `characters/sobaya/rig_v3_20260917/sobaya_rig.blend`

`tools/build_sobaya_v3_rig.py` の通常実行と `-- --rig` で再生成できる。編集用正本 `sobaya_animated.blend` や配布GLBとは区別する。

個別QA PNG、`motion_*.png`、Blender番号付きバックアップも対象を限定して除外。すでに追跡しているQA画像や採用ファイルを追跡解除するものではない。新しい本番入力やコードを一括で隠す除外ルールは設けない。
