# 旧30秒案・非採用

2026-09-23、ユーザーの長尺3D方式への変更によりアーカイブ。本ディレクトリは設計履歴のみ。APIは未送信。旧設定内の素材相対パスは移動前のものなので、この場所から実行しない。

現行正本はエピソード直下の `script.md` と `remotion/src/set-study.tsx`。旧30秒プレビューは `remotion/out/layout-preview.mp4` にローカル保持。
