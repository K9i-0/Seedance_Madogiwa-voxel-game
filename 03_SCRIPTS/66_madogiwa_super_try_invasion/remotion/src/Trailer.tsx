import React from 'react';
import {AbsoluteFill,Sequence,OffthreadVideo,staticFile,useCurrentFrame,interpolate} from 'remotion';
import m from './trailer-manifest.json';
const Card:React.FC<{text:string;duration:number}>=({text,duration})=>{const f=useCurrentFrame();const opacity=interpolate(f,[5,16,duration-10,duration-2],[0,1,1,0],{extrapolateLeft:'clamp',extrapolateRight:'clamp'});return <AbsoluteFill style={{background:'#000',justifyContent:'center',alignItems:'center'}}><div style={{opacity,color:'#f1efe9',fontFamily:'Hiragino Mincho ProN, serif',fontWeight:600,fontSize:34,lineHeight:1.9,letterSpacing:2,textAlign:'center',whiteSpace:'pre-line',padding:'0 45px'}}>{text}</div></AbsoluteFill>};
export const Trailer=()=> <AbsoluteFill style={{background:'black'}}>{m.segments.map((s,i)=><Sequence key={i} from={s.from} durationInFrames={s.duration}>{s.kind==='video'?<OffthreadVideo src={staticFile(m.source)} startFrom={s.sourceStart!} muted/>:<Card text={s.text!} duration={s.duration}/>}</Sequence>)}</AbsoluteFill>;
