# 生成前チェック — そば屋クローン研究所

2026-09-25。ユーザー依頼「生成前の最終準備」に対応。**本編API未送信／未課金**。

## 送信内容

| 項目 | 設定 |
| --- | --- |
| モデル | wan3.0-video |
| 尺・生成単位 | 30秒、単一タスク |
| 画角・解像度 | 16:9、480P |
| seed | 26092580 |
| 音声 | Wan生成、可視発話のリップシンクを優先 |
| 声質参照 | 福ちゃん7.45秒、やめ太郎7秒、合計14.45秒 |
| たこさん | 正典WAV未登録。落ち着いた中低域の成人男性研究者として生成指定 |
| BGM | なし。循環音・気泡・打音・警報・破砕・足音はWan生成 |
| 入力 | 人物シート4枚＋採用環境画像1枚、参照WAV2本 |
| 文字・測定UI | Wanには生成させずRemotionで合成 |
| 出力 | wan3_clone_lab_seed26092580_480p.mp4 |

`prompt_wan3.txt`が実送信文、`wan3_config.json`が生成設定。最後の採用セリフを一字も変更せず全文記載。30秒の内部カットは自然な発話に合わせる編集目安であり、台詞を引き伸ばしたり早送りしたりしない。

## 実行した確認

- 乾式検証成功：プロンプト12,452文字、参照画像5・音声2、入力約10.4 MiB。最大20,000文字以内。
- 全ての本番参照がエピソード内の実ファイル。人物シートはテンプレートとSHA-256一致。画像は全てrgb24、各辺240〜8,000px以内、各20MB未満。
- 正典WAVの出所とSHA-256・区間・実測尺を`input_manifest.json`へ記録。福ちゃんは正典0〜7.45秒、7.337823〜7.560476秒の低音量境界内で切り出し。やめ太郎は正典全7秒。参照音声の今回の切り出しは通し試聴未実施で、境界の機械検出と正典出所を検証したもの。
- 参照順Image 1〜5、Audio 1〜2をプロンプトと照合。やめ太郎の顔固定共通文を全文展開。そば屋の仮面装着、手にジョッキを出さない指定を明記。
- 序盤の前兆→測定→ホログラム→回収命令→警報・破砕→最終台詞の順序を確認。最後は言い切ってから暗転。
- Remotion TypeScriptチェック・UI静止画レンダー成功。既存UIデザインは維持し、30秒の`CloneLabFilm`へ重ねる構成を追加。
- APIキーは存在有無だけ確認し、値は表示・記録していない。モデル利用権限・課金状態のライブ呼び出し検証はしていない。

## 仕様・費用確認

- Qwen Cloudモデル・価格： https://www.qwencloud.com/models/wan3.0-video
- Qwen Cloud API： https://docs.qwencloud.com/api-reference/video-generation/wan30-video/create-task
- Alibaba CloudのAPI仕様補助確認： https://www.alibabacloud.com/help/en/model-studio/wan3-video-generation-api-reference

Qwen Cloud APIページはブラウズツールでタイムアウトしたが、同URLをcurl取得して本文を確認。media.urlは公開URLまたはBase64を受け付け、参照音声の合計は15秒以内。SDKではなくリポジトリの既存Qwen Cloud送信スクリプトを使用する。

Qwen Cloudモデルページの現在表示は480P **$0.035/秒**（通常$0.05/秒、30% off表記）。30秒で **$1.05**、通常単価なら$1.50。割引の終了日は未記載。これは表示価格の見積もりであり、実行直前に単価・適用条件を再確認する。今回有料実行の承認は求めず、準備だけを完了する。

## 次回生成時

まず乾式検証を再実行：

```sh
python3 -u .claude/skills/wan-video/scripts/qwen_wan3_generate.py 03_SCRIPTS/80_sobaya_clone_lab/wan3_config.json
```

ユーザーが有料生成を依頼・承認した後に限り、同じコマンドへ`--submit`を付ける。目安約8分、同じタスクをポーリングし重複送信しない。

## 生成後の編集

1. 台詞・声質・口の同期、仮面と人物同一性、ポッド一槽一体、破砕前後の連続性を通し確認。
2. 監視映像の実際の開始・終了を計測。予定は11〜16秒だが、生成時にずれる可能性がある。`remotion/src/production-edit.json`の開始フレームと必要ならUI側の長さを実カットへ合わせる。
3. 元MP4を`remotion/public/input.mp4`へhardlinkまたは実コピー。上書きが必要なら採用テイクを確認して行う。
4. `npm run render:film`で`final_remotion_clone_lab.mp4`へ出力。UIは1080pのまま、480p素材を拡大して合成する（素材自体の細部は増えない）。
5. 発音破綻があれば正常区間を保持し、必要な語句だけを修復・再監査。新規生成音声は未実測なので自然な30秒への収まり・声の再現は生成後の確認事項。

本編入力がない状態では完成版レンダーは明示的に停止する。架空の生成結果を完成版として出力しない。
