# 2026-09-26 音声修復・ホラー音響・タイトル追加

ユーザーから「興味深い」が「きょうみぶだかい」と聞こえるとの指摘。Irodoriのピンポイント修正、怖さを強める効果音、そば屋の声のタイトルコールを依頼された。元の30秒版は保持し、末尾へ3秒追加した33秒の改善候補を制作。

## たこさんの語句修復

- 参照：今回のWan原版11.6〜16.2秒のたこさんの発話を48kHzモノラルで抽出した `takosan_repair_reference.wav`。正典VOICE_CASTは変更しない。
- Irodori `Aratako/Irodori-TTS-v4.1-Small`、seed42、captionなし、本文CFG5、duration_scale1、UNCUT1。
- 生成本文「二人とも、実にきょうみぶかい。」、候補 `takosan_repair_sentence.wav` は3.92秒。
- 元音声18.36〜19.36秒（48kHzサンプル881280〜929279）だけを、生成音声2.10〜3.10秒（100800〜148799）へ等尺置換。一定ゲイン−0.4dB。ステレオ維持、時間伸縮・音素上クロスフェードなし。
- 文脈ごと生成し「興味深い」だけを挿入。前後は元の「二人とも、実に」および「そば屋くん、傷つけずに回収したまえ」を維持。
- 語句置換工程の区間外PCM完全一致、挿入PCMは生成素材の一定ゲイン適用後と一致。後続時刻は不変。その後の音響追加ミックスは別工程。
- ASR：元「2人とも、実に興味ぶだかい。」→修正「2人とも、実に興味深い。」。置換区間のみでも「興味深い」と認識。ASRのタイムスタンプと低音量境界を切出候補の根拠にした。ASRを聴感・リップシンクの合格とは扱わない。

再生成例（リポジトリルート）：

```sh
IRODORI_UNCUT=1 IRODORI_CFG_SCALE_TEXT=5 tools/irodori_speak.sh '二人とも、実にきょうみぶかい。' .local/takosan_repair_candidate.wav 03_SCRIPTS/80_sobaya_clone_lab/takosan_repair_reference.wav 42
```

## 効果音

今回のWan原版の24.133秒付近の破砕・衝撃音を切り出して加工・再配置。新たな効果音合成や追加の有料生成は実施していない。元の環境音と台詞はベースに維持。

- 8.60秒：遠いガラスの打音（ローパス650Hz、−18dB）。
- 11.10秒：やや近い打音（ローパス1000Hz、−14dB）。
- 23.00〜23.233秒：命令後の音量を短く落とす（−14dB、前後1フレームのゲインランプ）。
- 24.00秒：破砕の低音を補強（ローパス300Hz、−10dB）。
- 30.067秒：タイトル登場の衝撃音（ローパス1600Hz、−10dB）。

整数フレームの正本は `remotion/src/horror-edit.json`。ミックスの実装は `remotion/build_horror_audio.py`。効果音だけに端点フェードを使用し、台詞の音素をクロスフェードしない。

## そば屋ハザード

30〜33秒へ既存ロゴをRemotionで表示。画像はエピソード49の `logo_sobaya_hazard_master.png` と同一。30.30秒にそば屋の声で「そば屋ハザード」。

タイトル音声は `04_GAME_ASSETS/audio/hazard/voice/2b3687b44fb1d838.wav` の採用済み音源をエピソードへ実コピー。Irodori v4.1-Small、seed42、正典そば屋参照・モンスター化処理済み。1.38158秒、一定ゲイン−1.5dB。ASR「そばやハザード」。新規ナレーターへ変更しない。

## 出力・検証

- `final_remotion_clone_lab_horror.mp4`：33秒改善候補。`CloneLabHorror`、1920×1080 / 30fps、元映像の細部は480P。
- `final_remotion_clone_lab.mp4`：前回30秒版を保持。
- `review.html`：前回版と今回の改善候補を16.2秒から比較再生。タイトルコールと全編再生ボタンも配置。
- TypeScriptチェック成功。タイトルの文字、末尾、修復区間の静止画を出力。PCMピーク約−3.11dBFSでクリッピングなし。
- 通し試聴・声質接続の自然さ・リップシンクの合格は未判定。比較ページで確認できる状態にする。序盤の顔変形と大型投影装置は元映像のまま。

再現：`remotion/`で `npm run render:horror`。音声構築には既存 `.local/Irodori-TTS/.venv/bin/python` のNumPyを使用（`HORROR_AUDIO_PYTHON`で代替可能）。入力MP4は `public/input.mp4` へ原版をhardlinkまたは実コピーしておく。ロゴはレンダー時に `public/` へ実コピー。音声を33秒PCMとして構築し、Remotionの映像へFFmpegで一回だけAACエンコードしてmuxする。

最終MP4検証：映像・音声とも33.000秒、990フレーム、全編デコード成功。最終AACとミックスPCMの相互相関による遅延は0サンプル（8kHz測定）。比較ページはHTTP200とChromeでの修復台詞区間の再生表示を確認。音を聴いた判定とは区別する。

## A採用と測定人物の静止化（同日追記・現行）

ユーザーが音声比較A（既存ゲームのVOICEVOX:Voidollを参照したIrodori）を採用。たこさんの全発話を同じ声へ統一する。旧33秒版は残し、現行は `final_remotion_clone_lab_takosan_a.mp4`。

- `takosan_a_reference.wav` はゲーム正本 `f9bade3c3cb8638e.wav` の実コピー。正典VOICE_CASTへは未登録。
- `takosan_a_hologram.wav` はユーザーが聞いたAの正規化済みWAVそのもの（6.60秒）。16.60秒から配置。新規の声質加工・速度変更はしない。
- `takosan_a_scan.wav` は同じ参照・モデル・seed42・captionなし・CFG5・UNCUT1・duration_scale1で「五千。そば屋の半分近いな。」を生成。3.28秒、12.80秒から配置。ASR「ごせん。そばやの半分近いな。」。
- 両音声とも−18LUFS / −2dBTP目標で正規化。旧たこさん音声は11.6〜23.233秒から除き、同じWanの冒頭の非発話環境音を低音量で敷き直す。元の声を二重再生しない。環境音のみループ接続を50msでクロスフェードし、台詞は時間伸縮・クロスフェードしない。
- 声の差し替え工程の区間外PCMは一致。効果音・そば屋タイトルコールは前回と同じ。
- 測定画像はWan原版348f（11.60秒）を `scanner_closed_mouth_frame348.png` として採用。福ちゃんとやめ太郎の閉口を目視し、348〜485f全区間で同じ画像を使用。UI数値・走査線・警告は従来どおりアニメーションする。
- ホログラム側の口の動きはWan映像を保持。Aの発話尺は元と異なり、完全な音素同期の合格は主張しない。
- 再現は引き続き `npm run render:horror`。`horror-edit.json`の`takosanVoiceA`が現行の音声正本で、旧`repair`は履歴として保持。

Aの語尾を保つため、破砕前の短い音量低下を23.20〜23.333秒（696〜699f）へ移動。台詞素材の末尾23.20秒より後だけへ適用。

現行版の検証：TypeScript成功、合成PNG432fと480fで両顔の画素が完全一致。最終MP4は映像・音声33.000秒、990フレーム、全編デコード成功。ミックスPCMと最終AACの相互相関で遅延0サンプル（8kHz測定）。

## 培養音だけの静かな研究所＋A背景タイトル（現行追記）

出力 `final_remotion_clone_lab_quiet_lab.mp4`、33秒。ユーザー指定により、警報前（0〜23.233秒）の背景をポッドの作動音・気泡音へ組み直す。前回追加した8.6秒・11.1秒のガラス打音を撤去。警報・破砕・逃走と末尾コールは維持。

- 原版0〜11.6秒をDemucs `htdemucs`、two-stems vocals、shifts0、CPUで分離。採用した48kHz stereo PCM16は `opening_dialogue_isolated.wav`。台詞の再生成・速度変更はしない。
- `pod_ambience_loop.wav` は原版0.15〜3.05秒の非発話部分。HP38Hz / LP5000Hz。背景RMS −36dBFS、ループ境界200msを環境音だけクロスフェード。声以外の序盤背景はこの素材だけへ置換。
- 序盤の声の直接音は元時刻を保つ。抽出音声の無発話域だけをマスクし、語頭・語尾を含む採用窓は97〜176f、180〜250f、255〜347f。窓端10msで処理。
- 既存発話への室内反響処理：プリディレイ38ms、RT60 1.6秒、ウェット−11dB、180〜4800Hz、初期反射64/93/131/179ms、左右3ms差。seed80の決定的なインパルス応答で畳み込み。独立した新規効果音・BGMを生成する工程ではなく、元の声の残響加工。
- 反響の尾は測定シーンに自然に残す。たこさんは採用Aの声を従来の位置で配置。二人の静止画像と動く測定UIは維持。
- 30〜33秒：`reference_lab_hologram_adopted.png`（採用A4）を背景にし、既存タイトル画像を下寄りへスクリーン合成。背景とロゴは別レイヤー、端をフェザー処理。たこさんの顔と両脇のポッドを見せる。そば屋のタイトルコールは維持。
- 旧A音声版・旧ホラー版は上書きせず保持。再現は `npm run render:horror`。NumPyに加え既存Irodori環境のSciPyを使用。採用分離WAVを保存しているため、通常レンダー時のDemucs再実行は不要。

音声分離の副作用と残響量の好みは試聴評価事項。ASR・レベル確認だけで聴感合格や無劣化とは記録しない。

最終検証：序盤3台詞を個別ASRで照合（語尾「どうすんねん」は従来同様「どうすんね」と認識）。通しASRには末尾のゼロ長反復が出たため、実発話の追加と断定せず各台詞で再照合。33秒・990フレーム・全編デコード成功。最終音声とPCMマスターの相互相関遅延0サンプル（8kHz）。合成タイトルの文字・背景・フェザー処理を最終MP4から抽出して目視。
