# やめ太郎・培養ポッド内視点 差し替え準備

2026-09-26。ユーザー採用画像を保存済み。準備のみ。有料API未送信、既存完成版・Remotion本番コードは変更なし。

## 入力と生成

- 採用画像：`reference_lab_pod_pov_adopted.png`。画像生成ツールの最終修正画像を無加工でコピー。
- 人物：Image 2 やめ太郎、Image 3 そば屋。Image 1の構図と色、人物シートの同一性をそれぞれ指定。
- 同一テイクの音声参照：`yametaro_pov_existing_voice_reference.wav`。元の分離音声3〜6秒、3秒・48kHz mono PCM16。声質参照として使用。元音声に合わせた音素駆動ではない。
- `wan3_pov_replacement_config.json` / `prompt_wan3_pov_replacement.txt`：480P、16:9、4秒、seed26092680、単一カット。4秒は差し替え素材単位で、本編全体の尺ではない。
- 台詞：「これ、全部そば屋さんのクローン？」。追加台詞なし。
- カメラ固定に近い微小な水中の揺れ、少量の気泡。顔やポッドの配置を変える移動なし。
- やめ太郎はガラス目前に1人。反対側の3基にそば屋が1体ずつ。広い床、縦に続くポッドの左右縁、下側の丸い縁なし。

## 編集候補

完成版 `final_remotion_clone_lab_title_tempo.mp4` の90〜179f（3.0〜6.0秒）を映像のみ置換。元の福ちゃんカットは173f（5.7667秒）からなので、発話前の7fだけ新カットへ含め、180fから元映像へ戻す案。直前のそば屋寄りから視点を反転する。

発話窓は既存編集97〜177f。元の台詞・培養音・反響を含む音声マスターは変更しない。4秒生成から前後の無言部分を使って3秒を選び、元の発話開始・主要母音・閉口を確認する。速度変更で無理に合わせず、合わない場合は未合格として扱い、現行版へ自動採用しない。生成音声は同期比較用で、完成音声へ無断で差し替えない。

参照音声を渡すだけでは、既存音声との完全リップシンクを保証できない。この制約を記録したうえでユーザーの既存音声維持方針を優先する。今回、専用の音声駆動工程は準備していない。

## 検証と実行境界

乾式検証成功：3709文字、画像3枚・音声1本、約5.5MiB。人物同一性の共通文を全文展開。画像と音声の出所・ハッシュは `pov_replacement_preparation.json`。

生成を依頼された時点で公式仕様・料金を再確認し、既存スクリプトへ `--submit` を付けて1回送信する。準備段階では課金していない。

生成後は反射による増殖、やめ太郎の顔・口形、3基のそば屋、琥珀色、下縁の再出現、発話同期を確認。採用後にだけRemotionへ無音映像として配置し、全体36.3秒と音声PCMの維持を検証する。


## 生成結果

タスク `52d6a764-79c0-43b7-ade3-ef085a06aed8` 成功。854×480、30fps、120f／映像4秒、全編デコード成功。`wan3_pov_replacement_seed26092680_480p.mp4`。サンプル静止画ではやめ太郎1人・奥のそば屋3体を確認。ただし約1.3秒以降から手前にそば屋の肩、後半には頭が生成され、一人称視点が崩れる。指定外のため本編へ未採用。追加の有料生成は未実行。既存音声との同期は未承認。


## クロップ候補

`final_remotion_pov_cropped.mp4`：4秒素材を開始1.08倍から2秒で約1.333倍へ拡大し、左上基準x=30/y=0で肩・頭を画面外へ除外する比較版。再現 `python3 remotion/crop_pov_preview.py`（FFmpegによる前処理）。生成音声を無再圧縮コピー、既存本編の音声ではない。サンプル確認では奥の3体を保持。後半は顎が画面下端に近く、顔全体の余白が不足する。本編へは未採用。
